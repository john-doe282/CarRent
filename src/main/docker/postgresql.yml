version: '2'
services:
  rental-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/rental/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=rental
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
